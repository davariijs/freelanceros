name: Web CI

on:
  push:
    paths:
      - "apps/web/**"
      - "packages/**"
    branches:
      - main
  pull_request:
    paths:
      - "apps/web/**"
      - "packages/**"
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v3
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm --filter @freelanceos/web lint

      - run: pnpm --filter @freelanceos/web test
